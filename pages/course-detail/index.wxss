/* pages/course-detail/index.wxss */
/* 课程详情页样式 */

page {
  background-color: #f5f5f5;
}

.page {
  min-height: 100vh;
  padding: 24rpx;
  padding-bottom: calc(env(safe-area-inset-bottom) + 24rpx);
}

/* ========== 加载 / 空状态 ========== */

.loading-wrap {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 200rpx 0;
}

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 200rpx 0;
}

.empty-icon {
  font-size: 100rpx;
  margin-bottom: 24rpx;
}

.empty-text {
  font-size: 28rpx;
  color: #999;
}

/* ========== 通用卡片 ========== */

.card {
  background-color: #ffffff;
  border-radius: 16rpx;
  padding: 28rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.06);
}

.card-title-bar {
  font-size: 28rpx;
  font-weight: bold;
  color: #aa00ff;
  margin-bottom: 20rpx;
  padding-bottom: 12rpx;
  border-bottom: 2rpx solid #f0e8ff;
}

.empty-section {
  font-size: 26rpx;
  color: #bbb;
  text-align: center;
  padding: 24rpx 0;
}

/* ========== 课程头部 ========== */

.course-header-card {
  background: linear-gradient(135deg, #f3e8ff 0%, #ffffff 100%);
}

.header-top {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 16rpx;
}

.course-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #1a1a2e;
  line-height: 1.4;
  flex: 1;
  padding-right: 16rpx;
}

.header-actions {
  display: flex;
  align-items: center;
  flex-shrink: 0;
}

.action-btn {
  height: 60rpx;
  line-height: 60rpx;
  padding: 0 28rpx;
  border-radius: 30rpx;
  font-size: 26rpx;
  font-weight: 500;
  text-align: center;
  border: none;
}

.action-btn::after {
  border: none;
}

.select-btn {
  background: linear-gradient(135deg, #c6f, #aa00ff);
  color: #ffffff;
  box-shadow: 0 4rpx 16rpx rgba(170, 0, 255, 0.25);
}

.quit-btn {
  background-color: #fff0f0;
  color: #fa4126;
  box-shadow: 0 4rpx 12rpx rgba(250, 65, 38, 0.15);
}

.disabled-btn {
  background-color: #f0f0f0;
  color: #999;
}

.course-meta-row {
  display: flex;
  align-items: center;
  margin-bottom: 12rpx;
}

.meta-label {
  font-size: 26rpx;
  color: #999;
}

.meta-value {
  font-size: 26rpx;
  color: #666;
}

.course-desc {
  font-size: 26rpx;
  color: #666;
  line-height: 1.7;
  margin-top: 8rpx;
}

/* ========== 章节列表 ========== */

.chapter-list {
  display: flex;
  flex-direction: column;
  gap: 0;
}

.chapter-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 0;
  border-bottom: 1rpx solid #f0f0f0;
}

.chapter-item:last-child {
  border-bottom: none;
  padding-bottom: 0;
}

.chapter-left {
  display: flex;
  align-items: center;
  flex: 1;
  overflow: hidden;
}

.chapter-index {
  width: 48rpx;
  height: 48rpx;
  background-color: #f3e8ff;
  color: #aa00ff;
  font-size: 24rpx;
  font-weight: bold;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  margin-right: 16rpx;
}

.chapter-info {
  flex: 1;
  overflow: hidden;
}

.chapter-name {
  font-size: 28rpx;
  color: #1a1a2e;
  font-weight: 500;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.chapter-meta {
  font-size: 24rpx;
  color: #999;
  margin-top: 4rpx;
}

.chapter-arrow {
  font-size: 36rpx;
  color: #ccc;
  flex-shrink: 0;
  margin-left: 16rpx;
}

/* ========== 学员表格 ========== */

.student-table {
  width: 100%;
  overflow-x: auto;
}

.table-header {
  display: flex;
  background-color: #f8f4ff;
  border-radius: 8rpx;
  padding: 12rpx 0;
  margin-bottom: 4rpx;
}

.table-row {
  display: flex;
  padding: 14rpx 0;
  border-bottom: 1rpx solid #f5f5f5;
}

.table-row:last-child {
  border-bottom: none;
}

.th,
.td {
  font-size: 24rpx;
  color: #666;
  text-align: center;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.th {
  font-weight: 600;
  color: #999;
}

.th-index,
.td-index {
  width: 60rpx;
  flex-shrink: 0;
}

.th-name,
.td-name {
  flex: 1;
  min-width: 100rpx;
  padding: 0 8rpx;
}

.th-serial,
.td-serial {
  width: 80rpx;
  flex-shrink: 0;
}

.th-date,
.td-date {
  width: 130rpx;
  flex-shrink: 0;
}

/* 日期颜色 */
.text-red { color: #fa4126; }
.text-orange { color: #ff9900; }
.text-blue { color: #0052d9; }
