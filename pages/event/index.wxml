<!-- 分层活动 -->
<block wx:if="{{eventGroupData.isNotEmpty}}">
  <event-group
    store-goods="{{ eventGroupData.storeGoods }}"
    invalid-good-items="{{ eventGroupData.invalidGoodItems }}"
    binddelete="onGoodsDelete"
  />

  <view class="gap" />
</block>
